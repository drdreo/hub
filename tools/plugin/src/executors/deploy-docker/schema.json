{
    "$schema": "https://json-schema.org/schema",
    "version": 2,
    "title": "DeployDocker executor",
    "description": "",
    "type": "object",
    "properties": {
        "projectId": {
            "type": "string",
            "description": "Project ID to deploy"
        },
        "dockerFile": {
            "type": "string",
            "description": "Path to the docker file to use"
        },
        "redeploy": {
            "type": "boolean",
            "description": "If the service should be redeployed"
        }
    },
    "required": []
}
