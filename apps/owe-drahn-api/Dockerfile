# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build owe-drahn-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t owe-drahn-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system owe-drahn-api && \
          adduser --system -G owe-drahn-api owe-drahn-api

COPY apps/owe-drahn-api/dist owe-drahn-api/
RUN chown -R owe-drahn-api:owe-drahn-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix owe-drahn-api --omit=dev -f install

CMD [ "node", "owe-drahn-api" ]
