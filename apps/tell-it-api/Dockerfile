# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build tell-it-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tell-it-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tell-it-api && \
          adduser --system -G tell-it-api tell-it-api

COPY apps/tell-it-api/dist tell-it-api/
RUN chown -R tell-it-api:tell-it-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tell-it-api --omit=dev -f install

CMD [ "node", "tell-it-api" ]